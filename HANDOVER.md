# 画像・プロンプト共有ボード 引き継ぎドキュメント

Stable Diffusion 等で生成した画像と、そのプロンプト情報を仲間内で共有するための WEB アプリ(タイトル: **Prompt Reel**)。本ドキュメントは現状の仕様・設計思想・運用方法をまとめた引き継ぎ資料である。

---

## 1. 全体構成

| 要素 | 実体 |
|---|---|
| フロントエンド | `index.html` 1ファイル(CSS / HTML / JS すべて内包)。静的ホスティング(GitHub Pages 等)に置く想定 |
| バックエンド | `Code.gs`(Google Apps Script)。ウェブアプリとしてデプロイ(実行: 自分 / アクセス: 全員) |
| データベース | Google スプレッドシートの `posts` / `comments` / `board` / `admin` / `dictionary` の5シート |
| 画像ストレージ | Google Drive の指定フォルダ。画像投稿は `FOLDER_ID`、リクエストの参考資料は `REF_FOLDER_ID` |
| 画像アイコン | `index.html` と同階層の `images/` フォルダ(`thumb_up.png` / `thumb_down.png` / `shopping_cart.png`) |

`index.html` 冒頭付近の `GAS_URL` に GAS ウェブアプリの URL を設定して接続する。

### 通信方式(重要)
- **読み取り**: GAS ウェブアプリへのクロスオリジン fetch は CORS でブロックされることがあるため、**JSONP**(`<script>` タグ読み込み、`?callback=` パラメータ)で取得する。`doGet` は callback があれば JavaScript として、なければ素の JSON を返す。
- **段階読み込み(重要・投稿数が増えても初回表示を速くするための仕組み)**: `doGet` は `stage` / `detailId` パラメータで返す範囲を絞れる。フロントはこれを使って3段階に分けて読み込む。
  - `?stage=parents`: **親の画像投稿**(`type=image` かつ `parentId` 無し)と `options` のみを返す。フロントはこれで画像投稿一覧を最初に描画し、その時点で読み込み中表示を解除する(`loadPosts`)。
  - `?stage=rest`: 親以外の全データ(子画像・プロンプト例・リクエスト・修正依頼・`comments`・`board`・`dictionary`・`options`)を返す。`loadPosts` が親の描画後に背後で自動的に呼び出し(`loadRestStage`)、完了したら静かにマージして各タブを再描画する。失敗しても読み込み中表示は出さず、15秒後に自動で再試行する。
  - `?detailId=<idまたはshareId>`: **投稿1件+それに直接紐づく投稿(ベースプロンプト/親/応えたリクエスト/対応した修正依頼、および逆方向にこの投稿を参照している投稿)+その投稿のコメント**をまとめて返す(`buildDetailPayload`)。`stage=rest` の完了を待たずに個別の投稿を開けるようにするための優先取得用。
  - パラメータ無し(`?callback=` のみ等)の場合は従来どおり `{ posts, comments, board, options, dictionary }` を全件返す(後方互換。古いフロントや他の連携先向け)。
  - フロント側は `posts` 配列に対して `ensurePostLoaded(idOrShareId)` を通じてこれらを使い分ける。`stage=rest` が完了済みならローカルデータをそのまま返し(通信なし)、未完了なら該当投稿を個別取得してマージしてから返す。一覧行のクリック・共有URLからの表示・詳細モーダル内の相互リンク(ベースプロンプト/親/リクエスト/修正依頼)は、すべてこの `ensurePostLoaded` を経由してから該当のモーダルを開く。**新しく「投稿を開く」導線を追加するときは、必ず `ensurePostLoaded` 経由にすること**(でないと `stage=rest` 完了前に該当データが存在せず、紐づけやコメントが空のまま表示されてしまう)。
  - 段階読み込みはシートの読み取り回数(≒GAS側の処理コスト)は増える(親取得時・残り取得時で `posts` シートを2回読む)代わりに、**1回あたりの応答サイズを絞ることで、遅い回線でもタイムアウトする前に最初の一覧が表示できる**ことを狙った設計。投稿数がさらに増えて `stage=rest` 自体が重くなってきた場合は、`stage=rest` 自体をさらに分割する(例: 画像の子だけ/プロンプト例だけ、のように種類ごとに追加のstageを設ける)ことを検討する。
- **書き込み**: `fetch` の POST。`Content-Type: text/plain;charset=utf-8` で JSON 文字列を送る(プリフライト回避のため)。`doPost` が `body.action` で分岐:
  - `like` / `download`: カウント更新(`LockService` で排他制御 `bumpCount`)。
  - `dislike`: **バッド数の増減**(`dislikeCount` 列。`like` と同じく `bumpCount` で排他制御)。
  - `contact`: 管理者への連絡メッセージを `admin` シートに追記。
  - `comment`: 作品コメントを `comments` シートに追記。
  - `board`: 掲示板メッセージを `board` シートに追記。
  - `boardVote`: 掲示板メッセージのグッド/バッド増減。
  - `adminAuth` / `adminUpdate` / `adminDelete` / `adminSetOptions` / `adminDeleteComment` / `adminDeleteBoard`: **管理機能**(すべて `password` 照合が必須)。
  - 上記いずれでもない = 新規投稿(画像 / プロンプト例 / リクエスト / 修正依頼)。
- **プロンプト辞書の更新は POST では行わない**。GAS 側の時間主導トリガー `syncDictionary`(既定 5 分毎、`setup` が設置)が投稿とは独立に走り、投稿に出現した新規プロンプト語だけを翻訳して `dictionary` シートに追記する(詳細は 2-5・7 章)。**投稿処理には辞書ロジックを一切挟まない**ため、投稿保存・画面表示の速度に影響しない。

---

## 2. データ設計

### 2-1. `posts` シート(画像投稿・プロンプト例投稿・リクエスト・修正依頼を混在)
列構成(`COLUMNS`、この順・全 21 列):

```
id, timestamp, imageUrl,
title, author, prompt, negativePrompt,
settingsJson, tags, memo,
shareId, downloadCount, likeCount,
type, basePromptId, parentId,
referenceImages, requestId,
fixContent, fixRequestId, dislikeCount
```

- `id`: UUID。`shareId`: 共有 URL 用の 6 文字コード。
- `settingsJson`: 生成設定のオブジェクトを JSON 文字列で保存。
- `tags`: カンマ区切り文字列。
- `type`: `'image'` / `'prompt'` / `'request'` / `'fix'`。**空欄は `'image'` 扱い**(列追加前の既存データとの後方互換)。
- `basePromptId`: 画像投稿が「ベースにしたプロンプト例」の id(**カンマ区切りで複数可**)。
- `parentId`: 画像投稿の親作品(バリエーション元)の id。
- `referenceImages`: **リクエスト投稿**の参考資料画像 URL の JSON 配列(`REF_FOLDER_ID` に保存された thumbnail 形式)。空欄は `[]` 扱い。
- `requestId`: 画像投稿が「応えたリクエスト」の id(1 件)。
- `fixContent`: **修正依頼投稿(`type='fix'`)の「修正依頼の具体的な内容」**。修正依頼以外では空。
- `fixRequestId`: **画像投稿が「対応した修正依頼」の id(カンマ区切りで複数可)**。プロンプト例の `basePromptId` と同じ複数選択の仕様。
- `dislikeCount`: **バッド数**。`options.dislike` がオンのとき、`options.dislikeThreshold`(既定 10)以上で**利用者からは強制非表示**になる(管理者モードでは表示され続ける)。

`prompt` 列はリクエスト投稿では「リクエスト内容」を格納する(接頭辞 `r` の入力欄 `rBodyInput`)。

**修正依頼投稿(`type='fix'`)は画像投稿と同じフォーム(`imagePostForm`)を流用する**。画像必須・プロンプト・設定・タグなどの扱いは画像投稿と同一で、違いは `fixContent` を持つことと、画像投稿一覧に既定で出ないこと。

フロントは全件を `posts` に読み込み、`imagePosts`(type が image。prompt/request/**fix** は除外)/ `promptPosts` / `requestPosts` / **`fixPosts`** に派生させて使う。

### 2-2. `comments` シート(作品コメント)
列構成: `id, postId, timestamp, name, body`。`postId` は対象投稿の id。`name` 空欄は表示時「匿名」。投稿削除時、その投稿に紐づくコメントも一括削除される。

### 2-3. `admin` シート(管理者専用の設定・受信箱)
- **B1**: 管理画面用パスワード(**空欄の間は管理画面にログイン不可**)。管理者が手で設定する。
- **B2**: オプション機能の設定 JSON(管理画面から自動保存。手動編集不要)。
- **A4 以降(実際は A5〜)**: 利用者から届いた連絡メッセージが `[日時] 名前: 本文` 形式で1行ずつ追記されていく。

### 2-4. `board` シート(掲示板)
列構成: `id, timestamp, name, body, goodCount, badCount`。利用者同士が自由にやり取りする掲示板メッセージ。`name` 空欄は表示時「匿名」。各メッセージには 👍(グッド)/👎(バッド)を1端末1票で付けられ、`goodCount`/`badCount` に集計される(`boardVote` アクション。localStorage `boardVotes` で自票を保持し切替可)。管理モードから 1 件ずつ削除できる(`adminDeleteBoard`)。オプション `board` がオフのときは掲示板タブごと非表示、オプション `boardVote` がオフのときはグッド/バッドのみ非表示(`body.opt-no-boardvote`)。

### 2-5. `dictionary` シート(プロンプト内部辞書)
列構成: `en, ja, keywords, genre`。プロンプト語(英語)→ 日本語訳の辞書で、**プロンプトデータバンクの訳とジャンル/キーワード検索の唯一のデータ源**。フロントは辞書を一切内蔵せず、`doGet` が返す `dictionary` を読むだけ(端末側キャッシュも持たない=全員が同じ訳を共有)。

- `en`: プロンプト語のキー。小文字・アンダースコアを空白に正規化した形で格納(フロント/GAS 双方の `dictKey` と一致)。
- `ja`: 日本語訳。**管理者が手で書き換え可能**(直せばそのまま全員に反映)。
- `keywords`: 検索用の別名・関連語(自由記述、任意)。例: `crouching` の行に「蹲踞, 相撲」。データバンクの絞り込み対象に含まれる。
- `genre`: ジャンル(例: `ポーズ` / `髪` / `ネガティブ`)。データバンクのカードにチップ表示され、絞り込み対象にも含まれる。
- `setup` 実行時、辞書が空なら約 420 語のシード(`SEED_DICT_BY_GENRE`、genre 付与済み・keywords 空)を書き込む。
- 語の追加・翻訳は `syncDictionary`(時間主導トリガー)が担当。`en` があって `ja` が空の行(手動追加など)は次回同期で自動翻訳して埋める。1 回あたり `DICT_SYNC_LIMIT`(既定 50)語まで(実行時間・翻訳クォータ対策)。外部翻訳は GAS 標準の `LanguageApp.translate` を使用。

### 投稿の 4 種類と 4 つの紐づけ
1. **画像投稿**(type=image): 画像 + タイトル / 投稿者 / プロンプト / ネガティブ / メモ / 生成設定 / タグ。
2. **プロンプト例投稿**(type=prompt): 画像なし。自作プロンプトを作品として共有する。ダウンロード機能はなく、グッドのみ。
3. **リクエスト投稿**(type=request): 「こういう画像を作ってほしい」という依頼。タイトル / 投稿者 / リクエスト内容(`prompt` 列)/ 参考資料画像(複数可)。**タグ・生成設定はなし**。グッド・コメントは可。
4. **修正依頼投稿**(type=fix): 「この画像のここを直してほしい」という依頼。画像投稿フォームを流用し、`fixContent` に依頼内容を持つ。既定では画像投稿一覧に出ない。

紐づけは 4 系統あり、目的が異なる:
- **ベースプロンプト紐づけ**(画像 → プロンプト例): 「このプロンプト例を参考に作った」という関係。**複数選択可**。プロンプト例の詳細下部に、それをベースにした画像作品が一覧表示される。
- **親子紐づけ**(画像 → 画像): 同じ設定で Seed 違いを量産したとき用。子は**画像投稿一覧に既定で表示されず**、親の詳細下部に「この作品のバリエーション」として一覧表示される。親の候補は「親を持たない画像投稿」のみ(階層は 1 段まで)。
- **リクエスト紐づけ**(画像 → リクエスト): 「このリクエストに応えて投稿した」という関係。**1 件のみ選択**。画像詳細に「応えたリクエスト」リンクが出て、リクエスト詳細下部に応えた画像作品が一覧表示される。
- **修正依頼紐づけ**(画像 → 修正依頼、`fixRequestId`): 「この修正依頼に対応して投稿した」という関係。**仕様はベースプロンプト紐づけと同様(複数選択可)**。画像詳細に「対応した修正依頼」リンクが出て、修正依頼の詳細下部に対応作品が一覧表示される。修正依頼機能がオフのときは選択 UI ごと非表示。

---

## 3. 画面構成(タブ)

上部タブで画面を切り替える(`switchTab`)。ヘッダー右上には**管理画面ボタン**(および管理モード中のみ**オプション設定ボタン**)がある。

タブの並び順は: 投稿 / 画像投稿一覧 / プロンプト投稿一覧 / リクエスト一覧 / **修正依頼一覧**(`fixrequest-only`)/ プロンプトデータバンク / 掲示板 / 本ツールの使い方 / はじめてガイド。

### 3-1. 投稿タブ
- 上部のモード切替ボタンで「画像を投稿」(デフォルト)/「プロンプト例を投稿」/「リクエストを投稿」/**「修正依頼を投稿」**(`fixrequest-only`。修正依頼機能オン時のみ表示)を切替(`setPostMode`)。
- **修正依頼モード**は画像フォーム(`imagePostForm`)を流用し、見出しを「修正依頼を投稿」に変え、タイトル直下の **`fixContentField`(修正依頼の具体的な内容)** を表示、代わりに「対応する修正依頼」ピッカーを隠す。画像モードではその逆。
- **画像フォーム**: ドロップゾーンに画像を置くと、PNG の `parameters` チャンクや JPEG/WebP の EXIF `UserComment` から生成情報を解析し、フォームへ**自動入力**する。アップロード後はプレビューが**詳細モーダルと同じ大きさ(`max-height:70vh`)**で表示され、右上の **✕ ボタンで取り消し**できる(編集中は取り消すと元画像に戻る)。
- 生成設定は `SETTINGS_FIELDS` 定義からグリッド生成(UI Preset / Checkpoint / Model hash / VAE / Text Encoder / Sampling Method / Schedule Type / Sampling Steps / Width / Height / Batch Count / Batch Size / Shift / CFG Scale / Seed / RNG / Beta schedule alpha / Beta schedule beta / Version の 19 項目)。既存投稿の値を候補に出す(下記プルダウン方式)。
- **ベースにしたプロンプト例(任意)** / **親にする投稿作品(任意)** / **応えるリクエスト(任意)** / **対応する修正依頼(任意・複数可、修正依頼機能オン時のみ)**: モーダルピッカーで選択。選択結果はフォーム上に**タグ状のチップ**で表示され、複数選択時は横方向に折り返して並ぶ(`base-prompt-display` は `flex-wrap`)。未選択時は何も表示しない。ベースプロンプト・修正依頼は複数、親・リクエストは 1 件のみ。
- タグ: 既存タグのクラウド + 新規タグ入力。**タグは画像投稿とプロンプト例で共通のプール**。
- **プロンプトフォーム**: 画像関連を除き画像フォームと同等(接頭辞 `p`)。
- **リクエストフォーム**(接頭辞 `r`): タイトル / 投稿者 / リクエスト内容 / 参考資料。一覧・詳細の見出しは `requestLabel()`(タイトル。空欄時はリクエスト内容の先頭1行。行見出しは `.row-title` の nowrap で必ず1行に収まる)を使い、タイトルがある行のみリクエスト内容のスニペットも表示する(空欄時は見出しが内容そのものなので重複させない)。参考資料は画像投稿と同じドロップゾーン(`.dropzone-compact`、ドラッグ&ドロップ対応・控えめサイズ)で複数追加でき、**クライアント側で canvas を使い最大 1280px へ縮小・JPEG(品質 0.82)へ変換**してからサムネイル表示する(`resizeToJpeg`)。サムネイルは右上の ✕ で削除(枠外に配置=見切れない)、画像クリックで `refLightbox` に拡大表示。送信時に base64 で送り、サーバーが `REF_FOLDER_ID` へアップロードする。状態は `requestRefs`(`{kind:'new',dataUrl}` / `{kind:'existing',url}`)で保持。
- **画像フォームのドロップ後プレビュー**もクリックで `refLightbox` に拡大表示できる(✕は前面配置)。
- **投稿モード切替**は独立したボタン(`.post-mode-tabs` / `.post-mode-btn`)で並べる。
- **投稿者名の入力欄**は `.author-field` で括られ、オプションで投稿者名がオフのとき非表示になる。

#### 候補プルダウン(datalist の代替)
生成設定・投稿者・絞り込みなどの補完付き入力欄は、ブラウザ標準の `list`(前方一致予測)を**使わない**。各入力欄はロード時に `attachDatalistDropdown` で `.dl-wrap` に包まれ、`list` 属性は `dataset.dlList` に退避される。右端の **▼ ボタンで全候補メニュー**(`.dl-menu`)を表示し、クリックで値を入れる。**手入力も可能**。
- 予測変換をやめた理由: 旧実装は「▼で内容を一時クリア → blur で復元」だったが、フォーカス遷移でバグを生んだため全候補メニュー方式に変更した。
- Chrome のパスワードマネージャー誤発火対策: 全テキスト入力に `autocomplete="off" ... data-form-type="other"` 等を付与しているのに加え、**`guardPasswordManager()` が「管理者パスワード欄(`#adminPasswordInput`)以外」の全 `input`/`textarea` を初期状態 `readonly` にし、初回フォーカス時に `readonly` を外す**(Chrome の自動入力/鍵アイコンはフォーカス時点で判定されるため、readonly だと付かない)。`MutationObserver` で動的生成される入力欄も自動的に対象化するので、入力欄テンプレート側の変更は不要。datalist 代替メニューは値を JS 代入するため readonly でも動作する。

### 3-2. 画像投稿一覧タブ
- 表示形式: リスト / パネル(メイソンリー風グリッド、`layoutPanelGrid`)。パネルは各画像の**元の縦横比**を保ったまま隙間を揃えて敷き詰める。`layoutPanelGrid` は行の**サムネイル(`.row-thumb img`)**から縦横比を読んで `grid-row-end: span N` を算出する。ここを `item.querySelector('img')` のように書くと、行内で先に現れる `.row-counts` のグッド/バッドアイコン(16x16)を拾ってしまい、**全カードが正方形になる**ので注意。ツールバー右側に「リスト表示 / パネル表示」の切替と、その**左隣にパネル用の表示サイズスライダー**(`panelSizeControl` / `panelSizeRange`)をまとめて右寄せ配置(`.toolbar-right`)。スライダーはパネル表示中のみ表示され、CSS 変数 `--panel-col`(列の最小幅)を書き換えて `layoutPanelGrid` を再計算する。
- ソートチップ: 日時 / グッド / ダウンロード / 投稿者 / UI Preset / Checkpoint。**グッド・ダウンロードのチップはオプションで非表示になりうる**。**投稿者チップは投稿者名オプションがオフのとき非表示**(`body.opt-no-author`)。
- 絞り込み: 投稿者 / UI Preset / Checkpoint のテキスト部分一致 +「**キーワード検索**」(タイトル + プロンプト + ネガティブ + メモを対象。スペース/全角スペース区切りで複数語、AND / OR / NOT 指定。`filterKeyword` / `keywordMode`)+「**絞り込みをクリア**」ボタン + タグクラウド(AND / OR / NOT)。キーワード検索欄はタグクラウドの直上に置く。
- **タグクラウドは1行のみ表示**し、あふれる分は隠す。表示順は `tagDisplayOrder()` が決め、**選択中のタグを先頭にまとめる**ので、「全て表示」モーダルで選んだタグが 1 行に切り詰められても必ず見える。未選択のときは `shuffledTagOrder()` のキャッシュ順をそのまま返すため、**「タグ選択をクリア」すると元の並びに戻る**。検索パネル内の「〜をクリア」ボタンはいずれも行末要素なので、`.search-row .clear-tags{ margin-left:auto }` で**全て行の右端に揃えて**いる(「絞り込みをクリア」と「タグ選択をクリア」が同じ軸に並ぶ)。投稿フォーム側のクリアボタンは `.search-row` の外なので影響しない。「全て表示」ボタンは1行目の右端(最後の可視タグの直後)に置き、クリックで全タグのモーダルを開く(`applyTagCloudClamp`。各クラウドは `.tag-cloud-wrap` 内で `display:contents`、ボタンは同 flex 行の末尾要素)。
- 各カードの**グッド数・ダウンロード数はカード右上**(`.row-counts`、絶対配置)に表示。パネル表示では非表示。
- リスト行のメタ情報は 日時 / 投稿者 に加え、生成設定に値があれば **UI Preset / Checkpoint** も表示する。
- タグの表示は場所を問わず常に名前順(`sortJa`)。
- **親を持つ投稿(子)は既定で表示されない。** ツールバーの「**子も全て表示**」トグル(`includeChildrenToggle` / 状態 `showChildren`)をオンにすると、子も含めてすべてを一覧に表示する(リスト・パネル両方。各投稿は個別に絞り込み対象となる)。
- **修正依頼投稿(`type='fix'`)も既定で表示されない。** 「子も全て表示」の右にある「**修正依頼作品も表示**」トグル(2つは `.row-right-toggles` で並び順行の右端にまとめて配置)(`includeFixToggle` / 状態 `showFixWorks`、`fixrequest-only`)をオンのときだけ、他と同様に一覧(パネル含む)へ混ぜて表示する。修正依頼機能がオフのときはボタンごと非表示。
- **バッドがしきい値に達した投稿は一覧に出さない**(`visiblePosts` / `isHiddenByDislike`)。管理者モードでは表示される。

### 3-3. プロンプト投稿一覧タブ
- 画像一覧と同等の機能をプロンプト例向けに並列実装(リスト表示のみ)。**キーワード検索**(`pFilterKeyword` / `pKeywordMode`。タグクラウド直上に配置)も同様に持つ。グッド数はカード右上、ダウンロードは無し。

### 3-3b. リクエスト一覧タブ
- プロンプト一覧の右に配置。プロンプト一覧と同等の作りだが**タグ機能はなし**(接頭辞 `r`)。ソートは日時 / グッド / 投稿者(`REQUEST_SORT_FIELDS`)。絞り込みは**キーワード検索**(タイトル + リクエスト内容を対象。スペース/全角スペース区切りで複数語、AND / OR / NOT 指定可)。ソートチップ列に**回答済みフィルタ**の3状態チップ(全件 → 回答済みのみ → 未回答のみ)を持つ。行にはリクエスト内容の**1行スニペット**(`.request-snippet`)・参考資料枚数・回答作品件数を表示する。**投稿者名オプションがオフのときは投稿者ソートチップを非表示**(`body.opt-no-author`、画像/プロンプト一覧も同様)。
- **リクエスト詳細**(`openRequestModal`): リクエスト内容、グッド、共有、参考資料ギャラリー(クリックで `refLightbox` に拡大)、コメントトグル、(管理モード時)編集/削除、最下部に「このリクエストに応えて投稿された作品」一覧(`requestId` で紐づく画像投稿)。

### 3-3c. 掲示板タブ
- 利用者同士のやり取り用。上部に投稿フォーム(名前任意 + メッセージ)、下に投稿一覧(新しい順、`comment-item` スタイルを流用)。管理モード中は各メッセージに削除ボタン。**オプション `board` がオフのときはタブごと非表示**(`body.opt-no-board`)。表示中にオフにされた場合は画像一覧へ退避する。

### 3-3d. プロンプトデータバンクタブ
- タブ順は「修正依頼一覧」と「掲示板」の間(`data-tab="databank"` / パネル `#tab-databank`。描画 `renderDatabank`)。
- 全投稿(画像 + プロンプト例)の `prompt` / `negativePrompt` を「,」で分解・正規化して集計し、**出現回数つき + 日本語訳つき**の一覧を出す。トークン処理は `cleanPromptToken`(重み記法 `(...)`・`:1.2`・`[...]`・`\(` を除去)/ `splitPromptTokens` / `databankKey`(小文字・`_`→空白の正規化キー)。
- 操作 UI(すべて `.mode-toggle` チップ + 絞り込み入力。ラベルは `.label.label-tight` で余白を詰める):
  - **ソース**: PROMPT / NEGATIVE / 両方(`dbSource`)。
  - **対象**: 両方 / 画像投稿 / プロンプト例(`dbScope`)。
  - **並び順**: 採用回数順 / 英語名順 / 日本語名順(`dbSort`。`sortDatabankRows`)。
  - **絞り込み**(`dbFilter`): 英語(トークン)・日本語(訳)・`keywords`・`genre` のいずれかに部分一致。
- 一覧は **1 行 2 枚のカードグリッド**(`.databank-table` を 2 カラム、狭幅で 1 カラム)。各カードは トークン(+`genre` チップ)/ 出現回数 / 日本語訳。訳は `dictionary` シート由来(`internalDict[key].ja`)。未登録は「—」。
- **カードクリック時の挙動は 2 モード**:
  - 通常時 → `openDatabankUsage`。そのトークンを使っている作品を専用モーダル(`dbUsageOverlay` / `dbUsageBody`)に一覧表示(画像は `imageRowHtml`、プロンプト例は `promptRowHtml`、いずれも `counts:false`)。作品クリックで該当タブに移動し詳細を開く。
  - **カートに入れるモード**(`dbCartModeBtn` / 状態 `dbCartMode` / `body.db-cart-mode`。切替は `setCartMode()`)時 → クリックでトークンをカート(`dbCart`)に出し入れ。ボタンは**画面右下に固定表示**され、`body.tab-databank` が付いている間(= データバンクタブ表示中)だけ出る。
- **カート**(`dbCartBar`): **画面最下部に `position:fixed` で固定表示**し、スクロールしながら次々に追加できる。カードとは別デザイン(アクセント色の上ボーダー + 濃い背景 + 影)にして「カート」と分かるようにしてある。追加したトークンをチップ表示し、`COPY` で `A, B, C` 形式でまとめてコピー(`navigator.clipboard`)、チップの ✕ で個別削除、「空にする」で全消去。カートはページ内保持(リロードで消える)。
  - カートバー自体は画面幅いっぱいだが、内側(`.cart-inner`)は **`--content-max` / `--content-pad` を使ってメインエリア(`.wrap`)と左右端を揃えて**あるので、`COPY` / `空にする` ボタンは本文の右端にきれいに並ぶ。この 2 変数は `body` の padding と `.wrap` の max-width にも使っており、片方だけ変えてもズレない。チップ表示欄(`.cart-body`)は**チップ1行分(30px)を最小高さとして確保**しており、空のときと1個追加したときで高さが変わらない。`COPY` と `空にする` は押した瞬間だけ塗りつぶし(それぞれ `.copied` / `.cleared`、青と赤)になり文言が変わる。文言変化で幅が動かないよう `min-width` を持たせている。カートの実高さを `syncCartLayout()` が計測して `body` の CSS 変数 `--cart-h` に入れ、一覧の下余白(`body.db-cart-open #tab-databank` の `padding-bottom`)と右下ボタンの位置を追従させる。カート内容の増減・タブ切替・ウィンドウリサイズのたびに呼ぶ。
  - カートバーは `#tab-databank` の内側にあるため、**タブを離れると自動的に隠れる**(親が `display:none` になると `position:fixed` の子も消える)。
- 辞書はフロントに一切内蔵しない。`loadPosts` が `data.dictionary` を `buildInternalDict` で `key → {ja, keywords, genre}` に取り込むのみ。**外部翻訳・端末キャッシュはフロントに持たない**(翻訳は GAS 側 `syncDictionary` に集約)。

### 3-3e. 修正依頼一覧タブ
- タブ順は「リクエスト一覧」と「プロンプトデータバンク」の間(`data-tab="fixBrowse"` / パネル `#tab-fixBrowse` / 描画 `renderFixList`)。タブボタンは `fixrequest-only` で、**修正依頼機能がオンのときだけ存在が見える**。
- `fixPosts`(`type='fix'`)を一覧表示。行は画像サムネ + タイトル + 日時/投稿者/**対応作品 N件**(`fixAnswerCount`)+ 修正依頼内容のスニペット + タグ。
- 並び順チップ(`fSortKeys` / `renderFSortChips`、`REQUEST_SORT_FIELDS` を流用)とキーワード検索(`fFilterKeyword` / `fKeywordMode`、タイトル・修正依頼内容・プロンプト・タグが対象)。
- 並び順チップの末尾に **「対応済み」チップ**(`fAnsweredFilter`)がある。リクエスト一覧の「回答済み」(`rAnsweredFilter`)と同じ 3 状態トグルで、クリックのたびに 全件 → **対応済みのみ** → **未対応のみ** → 全件 と切り替わる。対応済みの判定は `fixAnswerCount(p) > 0`(その依頼に紐づく画像投稿があるか)。「絞り込みをクリア」でこの状態も解除される。
- 行クリックで通常の詳細モーダル(`openModal`)を開く。修正依頼の詳細では、画像の下に **「修正依頼の内容」**(`fixContent`)が表示され、最下部に **「この修正依頼に対応して投稿された作品」** が一覧表示される。
- 逆に画像投稿の詳細では、`fixRequestId` から **「対応した修正依頼」** リンクが出て、クリックで修正依頼の詳細へ移動する。

### 3-4. 本ツールの使い方タブ

**公開向けに全機能ガイドとして再構成済み**。パネル(`section.panel`)を縦に並べた構成で、末尾に管理者への連絡フォームを置く。目次やアンカーリンクは持たない(意図的に置いていない)。

構成(上から順):

| # | 見出し | オプション連動 |
|---|---|---|
| 1 | はじめての方向け:基本の使い方 | 文中のグッド/コメント言及のみ |
| 2 | このツールでできること | 修正依頼の項目のみ |
| 3 | タブごとにできること | 掲示板・修正依頼の項目 |
| 4 | 投稿するときに使える機能 | 投稿者名の項目 |
| 5 | 投稿どうしを紐づける | 修正依頼紐づけの項目 |
| 6 | 一覧から作品を探す | 並び順・絞り込みの該当語、修正依頼トグル |
| 7 | 詳細画面でできること | グッド / バッド / コメントの項目 |
| 8 | プロンプトデータバンクの使い方 | なし |
| 9 | 修正依頼の使い方 | **セクションごと**(`fixrequest-only`) |
| 10 | 掲示板の使い方 | **セクションごと**(`opt-only-board`)。投票の項目はさらに `opt-only-boardvote` |
| 11 | よくある質問(Q&A) | 該当 Q&A ごと |
| 12 | 管理者へ連絡する(`contactSection`) | なし |

- **先頭は「基本の使い方」(4ステップ: 投稿する / 見る / 探す / リアクションする)**。まずここだけ読めば使える構成にしてあり、セクション末尾に「以下は使いこなしたい人向け」と明記している。2 以降が全機能ガイド。
- Q&A は 20 問。削除・編集は管理者のみ / 匿名性 / 画像投稿とプロンプト例の違い / 派生作品の探し方 / 親子紐づけ / メタデータ自動入力(と読み取れない場合)/ リクエストと修正依頼の使い分け / 検索の AND・OR・NOT / タグクラウドの「全て表示」/ データバンクの訳 / グッド / バッド / 投稿が一覧から消えた場合 / 共有 URL / 読み込みが遅い場合 / 投稿してはいけない内容、など。

#### 見出しのスタイル(説明ページ専用)
使い方タブとはじめてガイドタブは文章量が多く、既定の `.panel h2`(16px / 700 / 4px のアクセント棒)では本文の見出し(`.feature-name`)と見分けがつきにくかったため、**この 2 タブに限って** h2 を強めている。

```css
#tab-usage .panel > h2,
#tab-guide .panel > h2{
  font-size: 19px; font-weight: 900; letter-spacing: .02em; gap: 12px;
  padding-bottom: 14px; margin-bottom: 18px; border-bottom: 1px solid var(--line);
}
#tab-usage .panel > h2::before,
#tab-guide .panel > h2::before{ width: 6px; height: 22px; border-radius: 3px; }
```

- 文字サイズ・太さの階層は **見出し 19px/900 → 項目名 13.5px/700 → 本文 13.5px/400**。さらに見出しだけが全幅の下線を持ち、アクセント棒も 6×22px と一回り大きい。
- **セレクタは `#tab-usage` / `#tab-guide` に限定している。** 投稿タブ・掲示板タブなど他の `.panel h2` は 16px/700・下線なしのまま。全体に広げたい場合は `.panel h2` 側を直す。
- 直下の子(`> h2`)のみが対象。パネル内にさらに入れ子の見出しを作る場合は対象外になる。

#### 本文のマークアップ2種類
使い方タブの本文には、用途の違う 2 つの形式がある。**順番に手を動かす手順は番号つき、機能を並べて紹介する箇所は機能リスト**という使い分けにしてある。

1. **手順(`.guide-step` + `.num`)**: 丸番号つき。**現在は先頭の「はじめての方向け:基本の使い方」だけで使う**。番号を振らない導入文(各セクションのリード文)も `.guide-step`(`.num` なし)で書ける。
2. **機能リスト(`.feature-item`)**: 見出し行 + 説明文 + 項目間の区切り線。**「このツールでできること」以降のセクション全部で使う**(Q&A を除く)。見出しを拾い読みできるようにするためのもの。
3. **Q&A(`.qa-item`)**: 「よくある質問」セクションのみ。Q./A. の接頭辞つきで、機能リストとは別系統のまま維持している。

```html
      <div class="feature-item">
        <div class="feature-name">共有</div>
        <div class="feature-desc">その作品を直接開けるURLをコピーできます。…</div>
      </div>
```

- `.feature-name::before` の 4px のアクセント棒は `.panel h2::before` と同じ視覚言語。棒(4px)+ gap(8px)= 12px を `.feature-desc` の `padding-left` と一致させてあるので、**見出しの文字と説明文の文字が同じ左端に揃う**。この 2 つの数値は連動しているので、片方だけ変えるとずれる。
- 区切り線は **`.feature-item + .feature-item{ border-top }`** の隣接セレクタ方式。項目の「間」だけに線が入るので、リストの後ろに `.guide-note` が続いても末尾に線が残らない。Q&A の `border-bottom` + `:last-child` 方式とは異なるので混同しないこと。
- 各セクションのリード文は `.guide-step`(`.num` なし)のまま置く。`.guide-step + .feature-item{ padding-top:0 }` でリード文直後の間延びを抑えている。
- オプション連動のクラスは `.guide-step` と同様に外側の `.feature-item` に付ける。
- **注意: セクションの 1 件目にオプション連動の項目を置かないこと。** 1 件目が CSS で消えると、2 件目の `border-top` がリストの先頭に残って浮いた線になる(CSS では非表示の兄弟を飛ばせない)。現状はどのセクションも 1 件目が非連動なので問題は出ていない。

#### オプション連動の仕組み(使い方タブの表示制御)
`applyOptionsToUi()` が `body` に付けるクラスに、**説明文側を対応させるための共通クラス**を用意している。JS 側の追加処理は不要(CSS のみで完結)。

```css
body.opt-no-like .opt-only-like,
body.opt-no-likecount .opt-only-likecount,
body.opt-no-dlcount .opt-only-dlcount,
body.opt-no-comment .opt-only-comment,
body.opt-no-author .opt-only-author,
body.opt-no-board .opt-only-board,
body.opt-no-boardvote .opt-only-boardvote,
body.opt-no-dislike .opt-only-dislike{ display:none !important; }
```

- 修正依頼だけは方向が逆(既定オフで、オンのときだけ見せる)なので、従来どおり `body:not(.opt-fixrequest) .fixrequest-only{ display:none !important; }` を使う。
- **2 つ以上のクラスを併記すると OR 条件になる**。例: グッド数のソートチップの説明は `class="opt-only-like opt-only-likecount"` にしてあり、どちらかがオフになれば消える。
- セクション全体がオプション対象のものは `<section>` 自体にクラスを付ける。これにより「見出しだけ残って中身が空」という状態を防いでいる。**新しくパネルを足すときも、中身が全部オプション依存なら必ずセクション側に付けること。**
- 文の一部だけを連動させたい場合は `<span class="opt-only-xxx">…</span>` で括る。括る範囲は**文単位**にし、消えたときに日本語として破綻しない形にする(句読点や接続詞だけが残らないように)。

### 3-5. はじめてガイドタブ
- 自分のパソコンで画像生成を始めるまでの手順を書いた読み物(`#tab-guide`)。準備するもの → Stability Matrix の導入 → Forge Neo のインストール → モデル(Checkpoint)の入手と配置 → 初回起動と生成 → LoRA の注意点、という流れ。
- **本ツールの機能とは独立**しており、オプション連動もしない。内容の更新は外部サービス(Civitai・Lykos)の状況に依存するため、リンク切れに注意する。

### 詳細モーダル(共通の `modalOverlay` / `modalBody`)
- **画像詳細**: 画像、グッド、バッド、ダウンロード、共有、PROMPT / NEGATIVE コピー、メモ、設定表、ベース/親/**応えたリクエスト**/**対応した修正依頼**リンク、子バリエーション一覧、**「コメント N件」トグル**、(管理モード時)編集/削除ボタン。
- **プロンプト例詳細**: グッド、共有、コピー、メモ、生成環境、派生作品一覧、コメントトグル、(管理モード時)編集/削除。
- **リクエスト詳細**: グッド、共有、リクエスト内容、参考資料ギャラリー、コメントトグル、回答作品一覧、(管理モード時)編集/削除。
- **修正依頼詳細**: 画像詳細と同じ構成に加え、画像の下に「修正依頼の内容」、最下部に「この修正依頼に対応して投稿された作品」。
- グッドボタンの配線は `wireModalLikeButton()` でガードしており、オプションでグッドがオフでもエラーにならない。
- 参考資料の拡大は専用の軽量ライトボックス `refLightboxOverlay`(`.full-image-overlay` スタイル流用)で表示する。

### 掲示板・その他モーダル
- 掲示板は専用モーダルを持たず、タブ内で完結(投稿フォーム + 一覧)。投稿は `postAction({action:'board'})`、削除は `adminDeleteBoard`。
- リクエストピッカー(画像投稿フォーム用)は `requestModalOverlay`。親ピッカーと同様の単一選択方式。

### コメントモーダル(`commentOverlay` / `commentModalBody`)
詳細モーダルの「コメント」トグルから開く**別モーダル**。**上に投稿フォーム(名前任意+本文)、下に投稿済みコメント一覧**の順。送信すると即座に一覧と詳細側のトグル件数を更新。管理モード中は各コメントに削除ボタンが出る。オプションでコメント機能がオフのときはトグル自体が出ない。

---

## 4. 管理モード

ヘッダー右上「管理画面」ボタン → パスワード入力モーダル → `adminAuth` で照合成功すると管理モードに入る(`adminPassword` を保持、`body.admin-on` 付与)。以降の管理系リクエストにはこのパスワードを毎回添えてサーバー側で照合する。

- **投稿の編集**: 詳細モーダルの「編集」で、**通常の投稿フォームを流用**して開く(専用モーダルではない)。投稿タブへ遷移し全項目をプリフィル、投稿ボタンが「保存する」に変わり「編集をキャンセル」ボタンが出る。画像は**差し替え可能**(ドロップで新画像、そのままなら現画像維持。差し替え時は旧 Drive ファイルをゴミ箱へ)。保存後は再読込して詳細を開き直す。
- **投稿の削除**: 確認ダイアログ後 `adminDelete`。子の `parentId`・他投稿の `basePromptId`/`requestId` 参照・紐づくコメントも自動で掃除する。リクエスト削除時はその参考資料画像も Drive のゴミ箱へ移す。
- **コメントの削除**: コメントモーダル内の削除ボタン → `adminDeleteComment`。
- **掲示板メッセージの削除**: 掲示板タブ内の削除ボタン → `adminDeleteBoard`。
- **オプション機能のオンオフ**: 管理モード中のみ表示される「オプション設定」ボタン → トグルスイッチのモーダル。対象は `like`(グッドボタン)/ `likeCount`(グッド数表示)/ `downloadCount`(DL数表示)/ `comment`(コメント機能)/ `author`(投稿者名表示。オフで全員「匿名」、かつ投稿者ソートチップを全一覧で非表示)/ `board`(掲示板タブ)/ `boardVote`(掲示板のグッド/バッド)/ **`fixRequest`(修正依頼機能)** / **`dislike`(バッドボタン)**。加えて数値項目として **`dislikeThreshold`(バッドの強制非表示しきい値、既定 10)** を入力できる。保存すると `admin!B2` に JSON 保存され、全利用者に反映。フロントは `applyOptionsToUi()` で `body` にクラス(`opt-no-like` / `opt-no-board` / **`opt-fixrequest`** / **`opt-no-dislike`** 等)を付けて CSS/描画を制御する。
  - 既定値は `like`〜`boardVote` が ON、**`fixRequest` と `dislike` は OFF**(`DEFAULT_OPTIONS`)。つまり**実装直後は修正依頼機能・バッドは非表示**で、管理者がオンにするまで利用者からは存在が見えない。
  - **`fixRequest` がオフのとき**は、修正依頼一覧タブ・投稿モード「修正依頼を投稿」・画像投稿フォームの「対応する修正依頼」・「修正依頼作品も表示」トグル・**使い方タブの「修正依頼の使い方」セクションと該当 Q&A** がすべて非表示になる(CSS `body:not(.opt-fixrequest) .fixrequest-only{display:none!important}`)。オンからオフに切り替えた際は、開いている該当タブ・投稿モード・トグル状態も `applyOptionsToUi` が自動で畳む。
  - **`dislike`(バッド) がオンのとき**、`dislikeCount` が `dislikeThreshold` 以上の投稿は全一覧・共有 URL から自動的に隠れる(`isHiddenByDislike` / `visiblePosts`)。**管理者モードでは隠れない**ので、内容を確認して削除するか判断できる。バッドの自票は localStorage `dislikedPosts` に保持し、もう一度押すと取り消せる(グッドと同じ挙動)。
  - **オプションをオフにすると、使い方タブ内の該当説明も同時に消える**(3-4 のオプション連動の仕組みを参照)。機能を追加・変更したときは、使い方タブ側の説明とゲートクラスも合わせて更新すること。

---

## 5. その他の仕様

- **グッド**: 1 投稿につき 1 端末(ブラウザ)1 回。`localStorage` の `likedPosts` で管理し、再クリックで取り消し。楽観更新 → サーバー結果で補正。
- **共有 URL**: `?p=<shareId または id>`。読み込み後 `openSharedFromUrl` が該当投稿の詳細を直接開く。
- **画像 URL**: Drive の `thumbnail?id=...&sz=w◯◯◯` 形式でサイズ別取得(`imageSrcFor`)。一覧 w320 / パネル w640 / 詳細 w1000、DL は原寸。
- **表示幅**: `.wrap` は `max-width:1600px; width:100%` でブラウザ幅に追従。詳細モーダルは `max-width:1100px`、狭いモーダル(管理ログイン/オプション)は `.modal-narrow`(480px)。
- **削除・編集は管理モード経由のみ**(一般利用者にはできない)。
- モーダル類は ESC キー / 背景クリックで閉じる(全モーダルが `closeXxxModal` を持ち、ESC ハンドラに登録済み)。
- 日本語ソートは `localeCompare(..., 'ja', { numeric: true })` ベース(`sortJa` / `compareWithKeys`)。
- **モーダルは必ず先頭から表示する**。`.modal-overlay` 自体がスクロールコンテナ(`overflow-y:auto`)なので、開くたびに `scrollTop = 0` に戻さないと、前に開いたモーダルのスクロール位置が残ってしまう。この処理は **`openOverlay(el)` に集約**してあり、モーダルを開く箇所はすべてこの関数を通す(`classList.add('open')` を直接書かない)。
- **右下の追従ボタン**(`.fab-stack`): 「先頭に戻る」(`scrollTopBtn`)を常時表示し、その左に「カートに入れるモード」をデータバンクタブ表示中のみ並べる。先頭に戻るボタンは、**モーダルが開いていればそのモーダルを、開いていなければページ本体を**先頭へスムーススクロールする。
  - 背景に埋もれないよう、**2px のアクセント枠 + 外周のダークハロー(`box-shadow` の spread)+ 濃いドロップシャドウ**で浮かせている。ハローが背後のカードとの境界を作るので、一覧の上に重なっても存在が分かる。ホバーで塗りつぶしに反転する。カートモードは ON のとき塗りつぶし(`.active`)で動作中を示す。
- **モーダル内の相互リンクではタブを切り替えない**。詳細モーダルはオーバーレイなので、背後のタブを変えてしまうと閉じたときに元の閲覧場所へ戻れなくなる(例: プロンプトデータバンク → 使用作品 → 画像詳細 と辿って閉じたら、データバンクではなく画像一覧に居る、という状態になっていた)。ベースプロンプト / 応えたリクエスト / 対応した修正依頼 / データバンクの使用作品のリンクは、いずれも `openXxxModal()` だけを呼ぶ。`switchTab` を伴ってよいのは、**投稿完了後の遷移・管理者編集(`post` タブへ)・共有 URL からの初期表示・オプションで非表示になったタブからの退避**に限る。
- **一覧行のグッド/バッドボタンの配線は `wireCountButtons(container)` に集約**している。行クリック(詳細を開く)への伝播を `stopPropagation` で止める必要があり、一覧ごとに個別配線すると漏れる(実際にプロンプト/リクエスト一覧でバッドを押すと詳細が開く不具合があった)。新しい一覧を足すときは必ずこの関数を呼ぶ。
- **UI 上の呼称は「グッド」「バッド」で統一**している(旧称「いいね」「よくないね」)。**内部のキー名・列名・アクション名は `like` / `likeCount` / `dislike` / `dislikeCount` のまま**なので、シートのデータや `admin!B2` の設定 JSON との互換性は保たれている。表示文言を変える際は内部キーを巻き込まないよう注意する。
- **グッド/バッドのアイコンは画像**を使う。`index.html` と同階層の `images/thumb_up.png`(グッド)、`images/thumb_down.png`(バッド)を描画する。投稿のグッド/バッドは `<img class="btn-icon">`(`countBadgesHtml`)、**掲示板のグッド/バッドも同じ画像**を `<img class="vote-icon">`(`boardVoteHtml`)で使う。**押下状態は CSS のみで表現**し、未押下は `opacity:.45` + `grayscale(1)`、押下時(`.liked` / `.disliked` / `.on`)は本来の色に戻す。したがって `updateCountUI` はクラスを付け替えるだけでよく、アイコン自体の差し替え処理は持たない。アイコンを差し替えたいときは `images/` の画像を置き換えるだけで全画面に反映される。
- **カートのアイコンも画像**(`images/shopping_cart.png`)を使う。カートバーの見出し(`.cart-icon`)と右下の「カートに入れるモード」ボタン(`.fab-cart-icon`)の 2 箇所。UI で使う画像アイコンは現時点で `thumb_up.png` / `thumb_down.png` / `shopping_cart.png` の 3 種類。
- **プロンプト辞書の運用**: 訳の修正・別名(`keywords`)やジャンル(`genre`)の付与は `dictionary` シートを直接編集する(全利用者に反映)。新規語の翻訳は `syncDictionary` トリガーが自動で行い、投稿から最大 5 分程度で反映される。大量の既存投稿を一気に埋めたいときは Apps Script エディタで `syncDictionary` を手動で数回実行する(1 回 `DICT_SYNC_LIMIT` 語ずつ処理)。`en` を手入力し `ja` を空にしておくと次回同期で自動翻訳が入る。
- **使い方タブは機能の鏡**。機能を足す/消す/オプション化するときは、必ず使い方タブの該当セクション・Q&A・ゲートクラスも同時に更新する。ここがずれると、オフにしたはずの機能の説明が利用者に見えてしまう。

---

## 6. 設計思想

1. **利用者が操作で迷わないことを最優先**。既存のデザイン・レイアウト・操作感を壊さない。新機能も既存 UI の文法(モーダル、チップ、タグクラウド、行スタイル)を流用して増やす。
2. **一覧を溢れさせない**。似た作品の量産は親子紐づけで畳み、プロンプト派生一覧からも子を除外する。
3. **後方互換**。シート列は末尾追記のみ。新列・新シートが無い/空でも既定値で動く(`type` 空=image、options 未設定=全 ON 等)。
4. **単一ファイル完結**。フロントは `index.html` 1 ファイルに CSS / HTML / JS を収める。外部依存なし(画像アイコンのみ `images/`)。
5. **既存コード非破壊・最小変更**。機能追加は共通ロジックを関数抽出(`compareWithKeys`, `imageRowHtml`, `wireImageRows`, `authorHtml`, `countBadgesHtml`, `attachDatalistDropdown`, `commentToggleHtml` 等)して並列実装する。
6. **コメント方針**: 説明コメントは書かない。セクション見出しコメント(`// ===== X =====` 形式)のみ許可。
7. **命名規則**: プロンプト側の並列実装は接頭辞 `p`。リクエストは `r`、修正依頼一覧は `f`。ピッカー系は用途名(`basePrompt◯◯`, `parent◯◯`)。管理系は `admin◯◯`。使い方タブのオプション連動の表示クラスは `opt-only-◯◯`。
8. **タグは全投稿共通のプール**(`allUniqueTags()`)。
9. **管理操作はサーバー側でパスワード必須**。フロントの管理モードはあくまで UI。実際の更新・削除は毎回 `admin!B1` と照合するため、フロントを改変されても書き込みはできない。
10. **オフにした機能は存在ごと見えない**。ボタンを消すだけでなく、タブ・投稿モード・紐づけ UI・使い方タブの説明まで一括で隠す。利用者に「使えない機能」を見せない方針。

---

## 7. デプロイ・運用手順

### 初回セットアップ
1. Google スプレッドシート新規作成 → 拡張機能 → Apps Script に `Code.gs` を貼り付け。
2. `FOLDER_ID` に画像保存先 Drive フォルダの ID を設定。`REF_FOLDER_ID` にリクエストの参考資料保存先フォルダの ID を設定(既定値あり)。
3. 関数プルダウンで `setup` を実行(権限承認 → `posts` / `comments` / `board` / `admin` / `dictionary` シートとヘッダー生成)。**`dictionary` は空なら約 420 語をシードし、辞書同期トリガー `syncDictionary`(5 分毎)を設置し、`LanguageApp`(翻訳)と `ScriptApp`(トリガー)の権限承認も走る**。承認ダイアログはすべて許可すること。
4. **`admin` シートの B1 に管理画面用パスワードを入力**(空欄だと管理画面にログインできない)。
5. デプロイ → 新しいデプロイ → ウェブアプリ(実行: 自分 / アクセス: 全員)→ URL を `index.html` の `GAS_URL` に設定。
6. `index.html` と `images/` フォルダ(`thumb_up.png` / `thumb_down.png` / `shopping_cart.png`)を静的ホスティングに配置。

### 更新時(毎回必須)
- `Code.gs` を変更したら「デプロイを管理」→ 鉛筆 → バージョン「**新バージョン**」→ デプロイ。**新しいデプロイを作ると URL が変わるので既存デプロイの更新を使う。**
- **シート列・シートを追加/変更したら `setup` を再実行**(`posts` のヘッダー上書き + `comments`/`admin`/`board`/`dictionary` の不足分を補完。データ行は触らない)。`setup` 再実行は辞書同期トリガーの再設置(既にあれば重複設置しない)と、`LanguageApp`/`ScriptApp` の権限承認も兼ねる。
- `index.html` はファイル差し替えのみ。
- 画像差し替え機能は Drive のファイル作成・削除権限を使うため、初回に権限承認が出たら承認する。

### 変更時の検証(最低限)
- `index.html` の `<script>` を抽出して `node --check`(構文)。`Code.gs` も `.js` にコピーして `node --check`。
- JS が `getElementById` / `querySelector('#...')` で参照する id が HTML(動的生成含む)に存在するか突合。
- フロントとバックエンドで対応するもの(`doGet` の返却キーと `loadPosts` の読み取り、プロンプトのトークン分解ロジック `cleanPromptToken`⇔`dictCleanToken` 等)がずれていないか確認する。
- **使い方タブを触ったら、`body` に `opt-no-*` / `opt-fixrequest` を手で付け外しして、全 ON と全 OFF の 2 パターンで表示崩れ・文の破綻・機能リストの最終項目の区切り線を確認する**(DevTools のコンソールで `document.body.classList.add('opt-no-board')` 等)。
- 可能なら Playwright 等でブラウザ実機のスモークテスト(パネル表示崩れ・パネル倍率スライダー・モーダル・コメント・オプション反映・プルダウン・データバンクのカード/使用作品/カートの回帰確認)。

### トラブルシューティング

- **症状: 管理モードでオプションを保存しても反映されない(全機能オフなど)。オプションモーダルに「保存に失敗しました: GAS側が新機能に未対応です…」と赤字が出る、または保存後にモーダルが閉じない。**
  - 原因はほぼ**デプロイのバージョン差**。`index.html` の `GAS_URL` が指すデプロイ済みGASが古いバージョンのままで、最新 `Code.gs` が反映されていない状態。`handleAdminSetOptions` の応答に `fixRequest` / `dislike` キーが含まれないと、フロントの `saveAdminOptions` が安全ガードで保存を中断する(古いGASの応答で新しい設定を黙って握りつぶさないための**意図的な**挙動)。該当ガードは「`['fixRequest','dislike']` が応答 `options` に含まれるか」を検査している。
  - **対処**: 7章「更新時(毎回必須)」の手順で、**既存デプロイを「新バージョン」として更新** → `setup` を再実行。**新しいデプロイを作ると URL が変わり別物になる**ので必ず既存デプロイの更新を使う。フロント側は変更不要。
- **切り分け: 「読み込み中…」が固まって進まない、と言われたとき。**
  - オプション保存(`saveAdminOptions`)・初回読み込み(`loadPosts`)・投稿後の再読み込み(`reloadAllPosts`)は、いずれも `try/finally` で必ず `hideLoading()` を呼ぶ。したがって**オプションのロジックが原因で読み込みオーバーレイが出っぱなしのまま固まることはない**。オーバーレイが本当に固まったままなら、原因はオプションではなく**通信側**(JSONP取得の失敗・`GAS_URL` の誤り・古い/未公開デプロイで `doGet` が応答しない・CORS 等)。まず DevTools コンソールの実エラーと、オプションモーダル内の赤いステータス文言を確認する。
  - 一方、**一覧の件数表示だけが「読み込み中…」のまま戻らない**のは別現象。`stage=rest`(第2段階の背後読み込み)が失敗して `restStageLoaded` が立っていない状態で、`loadRestStage` が15秒ごとに自動再試行し続けている(1-通信方式・`setListCountAndEmpty` 参照)。これも取得側の問題で、オプション設定とは独立している。

---

## 8. 既知の制約・注意点

- 投稿の編集・削除は**管理モード(パスワード必須)からのみ**。一般利用者は不可。
- グッド/コメントは端末・ブラウザ単位のため、別端末からは別カウント/別人扱い(名前は自己申告)。
- 親子は 1 階層のみ(子を親には選べない)。
- 紐づけ(basePromptId / parentId / requestId / fixRequestId)は管理モードでの編集を含め、投稿フォーム上で設定する。
- オプションで投稿者名をオフにすると、既存の投稿者名も含めて全員「匿名」表示になる(データ自体は消えない。再度オンで復活)。
- 連絡メッセージ・コメントには自由記述で個人情報が入りうる。管理者は `admin`/`comments` シートで内容を確認できる。
- GAS ウェブアプリの特性上、シートが大きくなると `doGet` の読み取り自体は重くなる。**初回表示については `stage=parents`(1-通信方式参照)で親の画像投稿だけを先に返すことでタイムアウトと体感速度をある程度緩和しているが、`stage=rest` で残り全部(子画像・プロンプト例・リクエスト・修正依頼・コメント・掲示板・辞書)をまとめて返す部分は依然として一括取得のままなので、辞書やコメントが肥大化するとそこがボトルネックになる**。さらに投稿数が増えて `stage=rest` 自体が遅くなってきた場合は、種類ごとに追加のstageへ分割する、辞書だけ別APIにする、といった対応を検討する。
- 画像は Drive の「リンクを知っている全員が閲覧可」に自動設定される(失敗しても投稿自体は成立)。
- リクエストの参考資料はクライアント側で JPEG・最大 1280px に縮小してから送るため、原寸は保持されない(容量対策)。参考資料は `REF_FOLDER_ID` に別保存され、リクエスト削除時のみゴミ箱へ移る(画像投稿本体の Drive ファイルは従来どおり削除時に残る)。
- 掲示板は誰でも匿名で投稿でき、削除は管理者のみ。連絡フォーム・コメント同様、本文に個人情報が入りうる。
- **`board` シートに 2 列(`goodCount` / `badCount`)を追加した回がある(掲示板のグッド/バッド)。既存スプレッドシートに未反映なら `setup` を再実行すること**(ヘッダーを 6 列に補完するのみで既存データ行は触らない)。
- **`posts` に 2 列(`referenceImages` / `requestId`)と `board` シートを追加した回がある。既存スプレッドシートに未反映なら `setup` を再実行すること**(ヘッダー補完のみで既存データ行は触らない)。
- **`posts` に 3 列(`fixContent` / `fixRequestId` / `dislikeCount`)を追加した回がある(修正依頼機能・バッド)。既存スプレッドシートに未反映なら `setup` を再実行すること**(ヘッダーを 21 列に補完するのみで既存データ行は触らない)。`fixRequest` / `dislike` オプションは**既定オフ**なので、使う場合は管理モードのオプション設定からオンにする。
- **`dictionary` シート(`en` / `ja` / `keywords` / `genre`)と辞書同期トリガー `syncDictionary` を追加した回がある。既存スプレッドシートに未反映なら `setup` を再実行すること**(辞書シート作成・約 420 語シード・トリガー設置・`LanguageApp`/`ScriptApp` の権限承認を行う。既存データ行は触らない)。
- プロンプト辞書の日本語訳は `LanguageApp` の**機械翻訳**がベースで、精度は目安。良い訳は `dictionary` シートで手直しすれば恒久反映される。`LanguageApp` には翻訳クォータがあるため 1 回の同期は `DICT_SYNC_LIMIT` 語までに制限しており、既存投稿が多い初回は全語が埋まるまで複数サイクル(各 5 分)かかる。
- プロンプト辞書はフロントに内蔵せず端末キャッシュも持たない設計(全員が `dictionary` シートの同じ訳を共有)。したがって辞書の見え方はシートの状態に一元的に依存する。
- **使い方タブのオプション連動は CSS のみ**。したがって「オプションがオフでも DOM 上には説明文が存在する」。ページのソースを見れば隠している機能の存在は分かるので、**秘匿が目的の情報は使い方タブに書かない**。
- 使い方タブの Q&A は `.qa-item:last-child` で最後の項目の下線を消しているが、CSS でオプション非表示になった項目も `:last-child` の判定対象のまま。最後の Q&A がオプション連動だと、その1つ手前の項目に下線が残る。**Q&A の最後には必ずオプション非依存の項目を置くこと**(現在は「投稿してはいけない内容はありますか？」)。機能リスト(`.feature-item`)は隣接セレクタ方式なので末尾側の制約はないが、**先頭側**に同種の制約がある(3-4 節を参照)。
- はじめてガイドタブは外部サービス(Stability Matrix / Civitai)の手順に依存するため、記載内容が陳腐化しやすい。公開後も定期的にリンクとバージョン名を確認する。

---

## 9. 使い方タブ掲載内容の要点(利用者向け)

- **基本の使い方**は 4 ステップ(投稿する / 見る / 探す / リアクションする)を維持。ここだけ読めば使える構成にしてある。
- 以降は全機能ガイド: タブごとの役割 / 投稿フォームの機能(メタデータ自動読み取り・▼ 候補プルダウン・タグ・参考資料)/ 4 つの紐づけ / 一覧の探し方(並び順・絞り込み・キーワード AND OR NOT・タグクラウド・パネル表示・各種トグル)/ 詳細画面(コピー・ダウンロード・共有・グッド・バッド・コメント・相互ジャンプ)/ プロンプトデータバンク / 修正依頼 / 掲示板。
- Q&A の重要事項:
  - 削除・編集は管理者のみ。削除依頼や不具合報告は最下部の連絡フォームから(共有 URL を添えると確実)。
  - **個人情報は管理者にも第三者にも伝わらない**: Google ログインを要求せず、投稿・閲覧はすべて匿名リクエストとして処理される。記録されるのはフォーム入力内容と画像のみで、IP やアカウントを管理者が知る手段は仕組み上ない。ただし本文に自分で書いた情報や画像メタデータは見える。
  - 投稿が一覧から消える条件(子作品 / 修正依頼 / バッドのしきい値到達 / 管理者削除)。
  - Googleドライブに保存される都合上、規約に触れる画像でサイト自体が終了しうること。
- 最下部に**管理者へ連絡する**フォーム(名前任意 + メッセージ、`admin` シートに追記)。
