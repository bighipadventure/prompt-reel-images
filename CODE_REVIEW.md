# Prompt Reel コードレビュー（バグ・リスク・改善案）

- **対象**: `index.html`（フロント 1ファイル）/ `Code.gs`（Google Apps Script）
- **実施日**: 2026-07-31
- **範囲・方法**: 静的コードレビュー（描画・エスケープ・認証・排他制御・段階読み込み・画像アップロード）＋「全オプションOFF」経路のDOMスタブ・シミュレーション。実GAS上の実行やブラウザ実機テストは含まない。

---

## 総評

現状のコードに**明確な機能バグは見つからなかった**。ミューテーション経路・認証・HTMLエスケープ・排他制御はいずれも妥当。存在するのは「バグ」ではなく、**規模拡大と公開運用にともなう潜在リスク**と、いくつかのハードニング（堅牢化）の余地である。以下はすべて「今すぐ壊れる」類ではなく、公開規模・悪用耐性を上げるための指摘。

---

## 1. 問題が見つからなかった点（確認済み）

| 観点 | 確認結果 |
|---|---|
| XSS（ユーザー入力のHTML描画） | prompt / negativePrompt / memo / fixContent / title / 投稿者名 / コメント名・本文 / 掲示板名・本文 / 設定表の値 / datalist候補まで、一貫して `escapeHtml` 済み。注入経路は確認できなかった。 |
| 管理操作の認証 | 管理系6ハンドラ（`handleAdminAuth` / `AdminUpdate` / `AdminDelete` / `AdminSetOptions` / `AdminDeleteComment` / `AdminDeleteBoard`）すべてが `checkAdminPassword` を通過。取りこぼしなし。 |
| 情報漏えい | `doGet` は posts / comments / board / dictionary / options のみ返却。管理パスワード（`admin!B1`）と連絡メッセージ（adminシート A5以降）は返さない。 |
| 排他制御 | カウント更新は `bumpCount` が `LockService` ＋読み書きで制御。コメント・掲示板の追記は `appendRow`（それ自体アトミック）。ロスト書き込みの実害は考えにくい。 |
| オプション全OFF | 保存・再描画・再読み込みの各経路をシミュレーションしたが、例外もハングも発生せず、読み込み表示も正常に解除された（＝オプションのロジックが原因で固まる経路は無い）。 |

---

## 2. 潜在リスク（優先度順）

| # | 項目 | 深刻度 | 内容 | 該当箇所（目安） |
|---|---|---|---|---|
| R1 | 公開後の無認証書き込み | 中（公開時） | GAS URL さえ分かれば誰でも投稿・コメント・掲示板・グッド/バッドを送れる。レート制限なし。Driveへ任意画像を投げ込めるため、ストレージ濫用や規約違反画像でサイトごと停止しうる。 | `doPost`（`Code.gs`）全体 |
| R2 | shareId 衝突 | 低（規模依存） | shareId は6桁（16進）で、生成時の一意性チェックが無い。投稿数増加で誕生日問題により衝突確率が上がり（数百件で1%前後）、衝突すると `?p=<shareId>` が別投稿を開きうる（先頭一致を返すため）。 | `Code.gs` `handleNewPost`（shareId生成）/ フロント `findLocalPost` |
| R3 | 本体画像のサイズ無制限 | 低〜中 | 参考資料のみ 1280px JPEG に圧縮。本体画像はメタデータ保持のため**意図的に無圧縮**。極端に大きいファイルは POST サイズ／実行時間の上限に当たり「投稿に失敗」となるが、原因が利用者に伝わりにくい。 | フロント: 本体は `fileToBase64`（無圧縮）、参考資料は `resizeToJpeg` / `REF_MAX_DIM=1280` |
| R4 | rest 段階の無限リトライ | 中（規模依存） | `loadRestStage` は失敗時に15秒ごとへ永久再試行し、上限が無い。データ肥大で `stage=rest` が重くなると、プロンプト例・リクエスト・掲示板・データバンクの各一覧が「読み込み中…」のまま戻らなくなりうる。 | フロント `loadRestStage`（`setTimeout(loadRestStage, 15000)`） |
| R5 | カウント水増し / バッド griefing | 低 | 1端末1票は localStorage 依存でクライアント側のみ。サーバは任意の投票を受けるため、グッド/バッドの水増しや、（dislikeオン時に）しきい値まで意図的にバッドして投稿を隠すことが理論上可能。 | `handleLike` / `handleDislike` / `handleBoardVote` |

---

## 3. 改善案（安いものから）

1. **shareId の一意化**: 生成時に既存と重複しないか確認して振り直す、または8〜10桁へ伸ばす。共有URLは id でも解決できるため致命ではないが、安価に潰せる。（R2）
2. **rest リトライ上限**: `loadRestStage` に再試行回数の上限を設け、打ち切り後は「再読み込み」ボタン＋ソフトエラー表示に切り替える。将来的には HANDOVER 記載どおり `stage=rest` を種類別に分割。（R4）
3. **画像アップロードのガード**: クライアント側で極端に大きいファイル（例: 15MB超）を弾き、明示メッセージを出す。再圧縮しない方針は維持でよい。サーバ側にも軽いサイズ／MIME チェックを足すと濫用に強くなる。（R3・R1）
4. **公開運用時の投稿抑止**: 投稿・掲示板に管理パスワードとは別の簡易な合言葉トークンを要求する、または投稿頻度の軽い制限を検討。無認証設計を保つ場合でも、主要フィールドにサーバ側の入力長上限を設けると暴走入力に強くなる。（R1）
5. **カウント整合性**: 匿名設計上、サーバ側で1端末1票を厳密化するのは難しい。割り切るか、投票に軽い署名トークンを導入するかを方針として決めておく。（R5）
6. **些細点**: `handleNewPost` のみ `LockService` 未使用（`appendRow` はアトミックなので実害は薄いが、他ハンドラと揃えるなら統一しても良い）。

---

## 4. 前提・免責

- 本レビューは静的解析とシミュレーションに基づく。実際の GAS デプロイ上の挙動・ブラウザ実機での回帰は、HANDOVER 7章「変更時の検証」に沿って別途確認すること。
- 「深刻度」は公開規模・悪用可能性を加味した相対評価であり、友人内クローズド運用では R1・R5 の優先度は下がる。
